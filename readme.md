The website for The New Best Practices.

# Setup

Before cloning or pulling, install Git LFS:

```
$ brew install git-lfs
```

Install Hugo:

```
$ brew install hugo
```

# Development

Run the development server:

```
$ hugo serve
```

